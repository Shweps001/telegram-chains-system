# 🚀 БЫСТРЫЙ СТАРТ - Unified система

## 1️⃣ Запуск Unified аккаунтов (параллельно)

### Терминал 1 - Chain 1:
```powershell
cd d:\ss\cycle\chain
python unified/unified_chain1.py
```

### Терминал 2 - Chain 2:
```powershell
cd d:\ss\cycle\chain
python unified/unified_chain2.py
```

### Терминал 3 - Chain 3:
```powershell
cd d:\ss\cycle\chain
python unified/unified_chain3.py
```

---

## 2️⃣ Запуск бота управления

### Терминал 4 - Master Bot:
```powershell
cd d:\ss\cycle\chain
python master.py
```

---

## 3️⃣ Очистка аккаунтов через бота

1. Открыть Telegram → Найти бота master
2. `/start` → `🛠️ Утилиты`
3. `🗑️ Очистить аккаунт`
4. Выбрать Chain или "Очистить ВСЕ"
5. Подтвердить → Дождаться завершения

---

## 4️⃣ Проверка логов

### Последние 20 строк Chain 1:
```powershell
Get-Content logs/unified_chain1.log -Tail 20
```

### Последние 20 строк Chain 2:
```powershell
Get-Content logs/unified_chain2.log -Tail 20
```

### Последние 20 строк Chain 3:
```powershell
Get-Content logs/unified_chain3.log -Tail 20
```

### Мониторинг в реальном времени:
```powershell
Get-Content logs/unified_chain1.log -Wait -Tail 10
```

---

## 5️⃣ Остановка всех процессов

### В каждом терминале:
```
Ctrl + C
```

Или через Task Manager:
```powershell
taskkill /F /IM python.exe
```

---

## ✅ Проверка что всё работает

### 1. Проверить сессии:
```powershell
Get-ChildItem sessions\unified*.session
```

Должно быть 3 файла:
- `unified_chain1_session.session`
- `unified_chain2_session.session`
- `unified_chain3_session.session`

### 2. Проверить процессы:
```powershell
Get-Process python
```

Должно быть 4 процесса (3 unified + 1 master)

### 3. Проверить логи:
```powershell
Get-ChildItem logs\unified*.log
```

Файлы должны обновляться (смотреть LastWriteTime)

---

## 🔧 Устранение проблем

### "Session file not found":
```powershell
# Создать пустые session файлы
New-Item -Path "sessions\unified_chain2_session.session" -ItemType File -Force
New-Item -Path "sessions\unified_chain3_session.session" -ItemType File -Force
```

### "FloodWait":
- ⏳ Подождите указанное время
- 🔄 Скрипт автоматически продолжит работу
- 📊 Паузы увеличатся автоматически

### "No links in queue":
- 📥 Скрипт автоматически сканирует канал
- ⏰ Подождите 1-2 минуты
- 🔄 Если не помогло - проверьте канал источник

### "Can't join group":
- ✅ Это нормально (закрытые группы)
- 📬 Запрос отправлен, ждём одобрения
- ⏭️ Скрипт продолжит работу

---

## 📊 Мониторинг через бота

### Главное меню:
- 📊 **Статистика** - сколько групп в каждом chain
- 🗄️ **База данных** - история обработанных групп
- 🛠️ **Утилиты** - очистка и парсер

### Редактор ключевых слов:
- ⚙️ **Настройки** → `✏️ Ключевые слова`
- 📝 Можно редактировать все 3 типа слов
- 🔄 Изменения синхронизируются на все 3 chains

---

## 💡 Полезные команды

### Узнать сколько групп в аккаунте:
```python
# Через бота: 📊 Статистика
```

### Экспортировать базу данных:
```python
# Через бота: 🗄️ База данных → 📤 Экспорт
```

### Найти дубликаты в БД:
```python
# Через бота: 🗄️ База данных → 🔄 Дубликаты
```

---

**Всё готово!** 🎉

Запускайте скрипты и следите за логами!
